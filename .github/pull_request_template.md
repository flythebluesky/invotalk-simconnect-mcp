## Summary

<!-- What does this PR do? -->

## Checklist

- [ ] `go vet ./...` passes
- [ ] `go test -race ./...` passes
- [ ] Tested on Windows with MSFS running (if touching SimConnect code)
- [ ] New events/SimVars follow the existing catalog pattern
